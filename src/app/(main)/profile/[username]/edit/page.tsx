import { ProfileEditContent } from "@/components/profile/profile-edit";

export const metadata = {
  title: "Edit Profile",
  description: "Update your profile information",
};

export default function ProfileEditPage() {
  return <ProfileEditContent />;
}
