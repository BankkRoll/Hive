import { StreamingSettings } from "@/components/streaming/streaming-settings";

export const metadata = {
  title: "Streaming",
  description: "Connect your streaming accounts and manage live status",
};

export default function StreamingPage() {
  return <StreamingSettings />;
}
